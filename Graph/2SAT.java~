/* 2SAT
 ** V + E
 * \emph{Input:} implication graph G\\
 * \emph{Output:} \texttt{true} if solution exists, \texttt{false} otherwise
 */

//START
//We assume that ind(not a) = ind(a) + N, with N being the number of variables
//could however be changed easily
public static boolean 2SAT(Vertex[] G) {
	//call SCC
	double DFS(G);
	//check for contradiction
	boolean poss = true;
	for(int i = 0; i < S+A; i++) {
		if(G[i].comp == G[i + (S+A)].comp) {
			poss = false;
		}
	}
	return poss;
}
//END
